namespace LuchthavenApp
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // .Net core applicatie maken
            // Entity framework core toevoegen aan code-project
                // Entityframeworkcore (.sqlserver, .tools)

            // Maak een NUnit test project aan
                // nuget packages: NUNIt (zijn al toegevoegd), NSubstitute (basis en de analyzers), autofixture
                //              entityframeworkcore.sqlite (niet nog eens .core achteraan)
            // Voeg een project reference toe

            // Maken dataklasses aan + eventuele testen ervoor
            // Maak per dataklasse een repository aan, werk met een sqlite voor een in memory database bij de testen
                // Let op dat je altijd de ctor zonder parameters hebt
            // Maak de dbcontext aan (kopieer uit de les en pas naamgeving aan)

        }
    }
}
